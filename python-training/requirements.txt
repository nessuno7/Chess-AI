# Python deps for the gRPC PPO prototype (java-core server + python-training client).
#
# Setup (from repo root):
#   py -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r python-training/requirements.txt
#
# torch CPU build is pulled from the PyTorch index below (no GPU required).
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.12.0+cpu
numpy>=2.0
grpcio>=1.81
grpcio-tools>=1.81
protobuf>=6.31.1
setuptools<82  # torch 2.12 requires setuptools<82
